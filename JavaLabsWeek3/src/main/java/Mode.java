import java.util.Arrays;
/*
public class Mode {
    public static void main(String[] args) {
        int[] array = new int[args.length];
        for (int i = 0; i < args.length; i++) {
            for (int j = 0; j < args.length; j++) {
                int numInArray = Integer.parseInt(args[j]);
                if (numInArray == i) array[i] += 1;
            }
        }
        //Arrays.sort(array);
        //int newI = array[array.length - 1];
        //System.out.println("Mode: " + newI);
    }
}
*/